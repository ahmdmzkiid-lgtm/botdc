{
  "name": "discord-bot-dashboard-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:bot": "pnpm --filter bot dev",
    "dev:api": "pnpm --filter bot exec tsx watch src/api/server.ts",
    "dev:dashboard": "pnpm --filter dashboard dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
